# План реализации функций AI-обработки статей

## Цель
Добавить функциональность для трех кнопок: "О чем статья?", "Тезисы", "Пост для Telegram" с использованием OpenRouter AI API (модель Deepseek).

## Текущее состояние
- ✅ Парсинг статей (извлечение даты, заголовка, контента)
- ✅ Перевод статей через OpenRouter AI
- ⏳ Три кнопки AI-обработки (требуют реализации)

## Порядок действий

### Этап 1: Создание API роутов для AI-обработки

#### 1.1. Создать API роут для "О чем статья?" (summary)
**Файл:** `app/api/summary/route.ts`

**Действия:**
1. Создать POST handler, аналогичный `/api/translate/route.ts`
2. Принимать `content` (текст статьи) из запроса
3. Проверять наличие `OPENROUTER_API_KEY` в переменных окружения
4. Формировать промпт для генерации краткого содержания:
   - System message: "Ты профессиональный аналитик текстов. Создай краткое содержание следующей статьи на русском языке (2-3 абзаца)."
   - User message: "Создай краткое содержание следующей статьи:\n\n{content}"
5. Отправлять запрос в OpenRouter AI с моделью `deepseek/deepseek-chat`
6. Возвращать результат в формате `{ summary: string }`
7. Добавить обработку ошибок и логирование

**Параметры запроса:**
- `temperature: 0.5` (для более творческого подхода)
- `max_tokens: 1000` (достаточно для краткого содержания)

---

#### 1.2. Создать API роут для "Тезисы" (theses)
**Файл:** `app/api/theses/route.ts`

**Действия:**
1. Создать POST handler по аналогии с summary
2. Принимать `content` из запроса
3. Проверять API ключ
4. Формировать промпт для генерации тезисов:
   - System message: "Ты профессиональный аналитик. Извлеки ключевые тезисы из следующей статьи и представь их в виде нумерованного списка на русском языке."
   - User message: "Извлеки ключевые тезисы из следующей статьи:\n\n{content}"
5. Отправлять запрос в OpenRouter AI
6. Возвращать результат в формате `{ theses: string }`
7. Добавить обработку ошибок

**Параметры запроса:**
- `temperature: 0.4` (для более структурированного ответа)
- `max_tokens: 1500` (для списка тезисов)

---

#### 1.3. Создать API роут для "Пост для Telegram" (telegram)
**Файл:** `app/api/telegram/route.ts`

**Действия:**
1. Создать POST handler по аналогии с предыдущими
2. Принимать `content` и опционально `title` и `date` из запроса
3. Проверять API ключ
4. Формировать промпт для генерации Telegram-поста:
   - System message: "Ты копирайтер для Telegram-каналов. Создай привлекательный пост на русском языке на основе следующей статьи. Используй эмодзи, структурируй текст, добавь призыв к действию."
   - User message: "Создай Telegram-пост на основе следующей статьи:\n\nЗаголовок: {title}\nДата: {date}\n\nКонтент:\n{content}"
5. Отправлять запрос в OpenRouter AI
6. Возвращать результат в формате `{ post: string }`
7. Добавить обработку ошибок

**Параметры запроса:**
- `temperature: 0.7` (для более креативного подхода)
- `max_tokens: 2000` (для полноценного поста)

---

### Этап 2: Обновление клиентской части

#### 2.1. Обновить `app/page.tsx`

**Действия:**
1. Обновить функцию `handleAction` для обработки трех типов действий:
   - `summary` - вызов `/api/summary`
   - `theses` - вызов `/api/theses`
   - `telegram` - вызов `/api/telegram`

2. Для каждого типа действия:
   - Проверять наличие `parsedData` и `parsedData.content`
   - Если данных нет, показывать alert: "Сначала распарсите статью"
   - Устанавливать `loading: true` и соответствующий `actionType`
   - Отправлять POST запрос на соответствующий API endpoint
   - Передавать `content` (и опционально `title`, `date` для telegram)
   - Обрабатывать ответ и устанавливать результат в `result`
   - Обрабатывать ошибки

3. Обновить логику отображения результатов:
   - Для всех AI-действий отображать результат как обычный текст (не JSON)
   - Сохранить JSON-формат только для парсинга

**Пример структуры функции:**
```typescript
const handleAction = async (type: 'summary' | 'theses' | 'telegram') => {
  if (!parsedData?.content) {
    alert('Сначала распарсите статью')
    return
  }

  setLoading(true)
  setActionType(type)
  setResult('')

  try {
    const endpoint = `/api/${type}`
    const body: any = { content: parsedData.content }
    
    // Для telegram добавляем title и date, если есть
    if (type === 'telegram') {
      body.title = parsedData.title
      body.date = parsedData.date
    }

    const response = await fetch(endpoint, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    })

    if (!response.ok) {
      const error = await response.json()
      throw new Error(error.error || 'Failed to process')
    }

    const data = await response.json()
    const resultKey = type === 'summary' ? 'summary' : type === 'theses' ? 'theses' : 'post'
    setResult(data[resultKey] || 'Результат не получен')
  } catch (error) {
    setResult(`Ошибка: ${error instanceof Error ? error.message : 'Неизвестная ошибка'}`)
  } finally {
    setLoading(false)
  }
}
```

---

### Этап 3: Ограничение длины контента

**Действия:**
1. В каждом API роуте ограничить длину передаваемого контента:
   - Для summary: до 8000 символов
   - Для theses: до 8000 символов
   - Для telegram: до 10000 символов (можно больше, т.к. нужен полный контекст)

2. Добавить комментарии о лимитах токенов и возможной оптимизации

---

### Этап 4: Улучшение UX

**Действия:**
1. Добавить проверку наличия распарсенных данных перед показом кнопок AI-действий
2. Можно скрыть кнопки AI-действий до парсинга или показывать их неактивными
3. Добавить индикацию загрузки для каждого типа действия
4. Улучшить сообщения об ошибках

---

### Этап 5: Тестирование

**Действия:**
1. Протестировать каждую функцию отдельно:
   - Парсинг статьи
   - Генерация краткого содержания
   - Генерация тезисов
   - Генерация Telegram-поста

2. Проверить обработку ошибок:
   - Отсутствие API ключа
   - Неверный API ключ
   - Ошибки сети
   - Пустой контент

3. Проверить граничные случаи:
   - Очень длинные статьи
   - Короткие статьи
   - Статьи без заголовка/даты

---

### Этап 6: Оптимизация (опционально)

**Действия:**
1. Добавить кэширование результатов (если одна и та же статья обрабатывается повторно)
2. Добавить возможность сохранять результаты
3. Оптимизировать промпты для лучшего качества ответов
4. Добавить настройки температуры и max_tokens через UI (опционально)

---

## Технические детали

### Используемые технологии
- **Next.js 14** (App Router)
- **OpenRouter AI API** (модель: `deepseek/deepseek-chat`)
- **TypeScript**
- **React Hooks** (useState)

### Переменные окружения
- `OPENROUTER_API_KEY` - ключ API для OpenRouter (уже настроен)

### Структура файлов
```
app/
├── api/
│   ├── parse/
│   │   └── route.ts (✅ существует)
│   ├── translate/
│   │   └── route.ts (✅ существует)
│   ├── summary/
│   │   └── route.ts (🆕 создать)
│   ├── theses/
│   │   └── route.ts (🆕 создать)
│   └── telegram/
│       └── route.ts (🆕 создать)
└── page.tsx (🔄 обновить)
```

---

## Порядок реализации (рекомендуемый)

1. **Создать `/api/summary/route.ts`** - самая простая функция
2. **Обновить `handleAction` в `page.tsx`** для поддержки summary
3. **Протестировать summary**
4. **Создать `/api/theses/route.ts`**
5. **Обновить `handleAction` для theses**
6. **Протестировать theses**
7. **Создать `/api/telegram/route.ts`** (самая сложная, требует title и date)
8. **Обновить `handleAction` для telegram**
9. **Протестировать telegram**
10. **Финальное тестирование всех функций**

---

## Критерии готовности

- ✅ Все три API роута созданы и работают
- ✅ Клиентская часть обновлена и корректно вызывает API
- ✅ Обработка ошибок реализована
- ✅ Результаты корректно отображаются в UI
- ✅ Все функции протестированы
- ✅ Код соответствует существующему стилю проекта

